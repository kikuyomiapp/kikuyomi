/// Fakes, fixtures, an in-memory database and a fake clock.
///
/// Depended on only by other packages' dev_dependencies.
///
/// Pure Dart. This package must never import Flutter or a platform plugin.
library;
